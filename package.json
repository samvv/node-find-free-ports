{
  "name": "node-find-free-ports-workspace",
  "private": true,
  "workspaces": [
    "package",
    "test-package"
  ],
  "scripts": {
    "test": "concurrently 'cd package && npm test' 'cd test-package && npm test'"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
