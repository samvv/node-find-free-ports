{
  "name": "node-find-free-ports-test-package",
  "version": "1.0.0",
  "private": true,
  "description": "A small package to check that we can import our package",
  "main": "index.js",
  "scripts": {
    "test": "tsc && node index.js && node commonjs.js && node esm.mjs"
  },
  "dependencies": {
    "find-free-ports": "^3.0.0"
  },
  "keywords": ["example", "test", "networking", "cli"],
  "author": "Sam Vervaeck",
  "license": "MIT"
}
